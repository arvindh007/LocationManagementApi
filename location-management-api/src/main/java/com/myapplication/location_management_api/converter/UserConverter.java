/*
package com.myapplication.location_management_api.converter;

import com.myapplication.location_management_api.entity.UserEntity;
import com.myapplication.location_management_api.model.UserModel;
import org.springframework.stereotype.Component;

@Component

public class UserConverter {
    public UserEntity convertModelToEntity(UserModel userModel){
        UserEntity userEntity = new UserEntity();
        userEntity.setEmail(userModel.getEmail());
        userEntity.setFullname(userModel.getFullname());
        userEntity.setMobilenumber(userModel.getMobilenumber());
        userEntity.setPassword(userModel.getPassword());

        return userEntity;
    }
}
*/
